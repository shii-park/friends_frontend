# 2026-02-15 開発日記 (13)

## 実施内容
- ページ遷移アニメーションのブラッシュアップ
- `AnimatePresence` のモードを `wait` から `popLayout` に変更し、遷移前後の画面が同時に動くように設定
- ホーム画面からストレージ画面への遷移時、ホーム画面が上にスライドアウトし、同時にストレージ画面が下からスライドインする「押し上げ」演出を実装
- ストレージ画面からホーム画面へ戻る際は、その逆の動き（ホームが上から降りてくる）を実装

## 変更ファイル
- `src/App.tsx`: `AnimatedRoutes` 内の方向検知ロジックを強化し、`direction` を各コンポーネントに伝播させるよう修正
