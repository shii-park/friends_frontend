# 2026-02-15 開発日誌

## 作業内容
- API連携の基盤となるサービス層を導入 (`src/services/api.ts`)
  - バックエンドとの通信を想定した非同期インターフェース (`apiService`) を作成
  - 通信遅延（`sleep`）を模倣し、ローディング演出のテストを可能に
- `useUser` hook を非同期処理に対応するようリファクタリング
  - `isLoading` 状態を追加し、通信中のUI制御を可能に
  - 各アクション (`login`, `drawGacha`, `levelUpCard`) を `async/await` に対応
- 各画面を非同期API対応にアップデート
  - タイトル画面 (`Title.tsx`)：ログイン処理の待機とローディング中のボタン無効化
  - ガチャ画面 (`Gacha.tsx`)：抽選処理の非同期化と連打防止
  - ストレージ画面 (`Storage.tsx`)：強化処理の非同期化とローディング演出

## 完了した機能
- フロントエンドにおけるAPI連携の基本構造（非同期化とサービス分離）

## 次回への引き継ぎ
- `apiService` 内のエンドポイントを実際のバックエンドURLに書き換えることで、実データとの連携が可能。
- 次回は「モダンでキャッチーな見た目」をさらに追求するため、通信中のグローバルなローディング演出（オーバーレイ等）の導入や、ガチャ演出のさらなるブラッシュアップを検討。
