# 2026-02-14 開発日誌

## 作業内容
- `react-router-dom` を導入し、ルーティングの基盤を作成
- プロジェクトのフォルダ構造を整理 (`src/pages`, `src/components`, `src/types`, `src/hooks`)
- ゲームの基本型定義を作成 (`src/types/game.ts`)
- タイトルページ (`src/pages/Title.tsx`) の初期実装
  - ゲームタイトルの表示
  - 未ログイン時のユーザー名入力機能
  - ホーム画面への遷移ボタン
- ホーム画面 (`src/pages/Home.tsx`) の初期実装
  - ユーザー情報（RP、コイン、石）のヘッダー表示
  - バトル・ストレージ・ガチャへの導線となるメニューカードを配置
- 各ページのプレースホルダーを作成し、ルーティングを設定
- デザインの基礎適用 (`src/App.css`)
- ユーザー情報のグローバル状態管理 (`src/hooks/useUser.tsx`) を導入
  - `UserProvider` によるユーザー名、RP、コイン、所持カードの管理
  - タイトル画面でのユーザー登録処理の実装
  - ホーム画面でのコンテキスト利用へのリファクタリング
- ストレージ画面 (`src/pages/Storage.tsx`) の実装
  - キャラクターと装備を切り替えて表示するタブ機能を実装
  - 所持カードをグリッド表示するUIを作成
  - `UserProvider` にて初期配布カード（冒険者、錆びた剣など）のモックデータをデフォルトで保持するように変更
- 各種不具合修正
  - ストレージのパスを `/storage` に統一し、ドキュメントの誤字を修正
  - ストレージ画面が真っ暗になる、またはリダイレクトされる問題を修正
  - 未ログイン時でも開発しやすいよう、初期状態でモックユーザー情報をセット
- ガチャ機能の基盤実装
  - ガチャ画面 (`src/pages/Gacha.tsx`)：バナー表示と1連/10連ボタンを配置
  - ガチャ演出画面 (`src/pages/GachaEffect.tsx`)：召喚アニメーション（シミュレーション）を実装
  - ガチャ結果画面 (`src/pages/GachaResult.tsx`)：結果表示用のレイアウトを作成
  - ルーティングの設定とスタイリングの適用
- ガチャ実行ロジックの実装
  - `UserProvider` に `drawGacha` 関数を追加し、ドキュメントの確率に基づいた抽選処理を実装
  - 石（ガチャ石）の概念を導入し、ガチャ実行時に消費するように変更
  - 抽選されたカードを自動的に所持カード (`ownedCharas`, `ownedEquips`) に追加
  - ガチャ演出画面から結果画面へ抽選データを引き継ぐように修正
  - ガチャ結果画面で実際に引いたカードを表示するように実装

## 次の予定
- ホーム画面の実装
- ユーザー情報のグローバル状態管理の検討
- 各ページのデザイン適用
