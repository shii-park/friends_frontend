# 2026-02-26 の変更内容

- カードへのアイコン表示機能の実装
- `src/types/game.ts`
  - `Card` インターフェースに `cardIconUrl` プロパティを追加
- `src/components/GameCard.tsx`
  - `cardIconUrl` が存在する場合、 `src/assets` から画像を動的に読み込んで表示するように修正
  - `getImageUrl` ヘルパー関数を追加し、Vite の `import.meta.url` を利用した動的アセット読み込みに対応
- `src/App.css`
  - `.card-image-container` スタイルを追加し、カード画像の表示枠を定義
  - `.card-icon` スタイルを追加し、ホバー時に画像がわずかにズームする演出を導入
  - プレースホルダーと画像表示のスタイルを共通化
