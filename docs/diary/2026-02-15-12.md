# 2026-02-15 開発日記 (12)

## 実施内容
- ページ遷移アニメーションの導入
- `framer-motion` ライブラリの追加
- `App.tsx` に `AnimatePresence` と `motion` を使用した遷移ロジックを実装
- ホーム画面からストレージ画面への遷移時に、下からスライドインするアニメーションを設定
- ストレージ画面からホーム画面へ戻る際に、上からスライドダウンして戻るアニメーションを設定

## 変更ファイル
- `package.json`: `framer-motion` を追加
- `src/App.tsx`: アニメーション用コンポーネント `PageTransition` と `AnimatedRoutes` を追加
- `src/pages/Home.tsx`: ストレージ遷移時に `state` を渡すよう変更
- `src/pages/Storage.tsx`: ホーム遷移時に `state` を渡すよう変更
