# 2026-02-15 開発日記 (16)

## 実施内容
- ガチャ関連画面への遷移にズームアニメーションを追加
- `App.tsx` の `pageVariants` に `zoom` タイプ（スケールと不透明度の変化）を実装
- ホーム ↔ ガチャ ↔ 演出 ↔ 結果 の遷移において、奥に吸い込まれるような（または手前に出てくるような）ズーム演出を設定
- 全体的な画面遷移の統一感を向上（ストレージは上下、バトルは左右、ガチャはズーム）

## 変更ファイル
- `src/App.tsx`: `zoom` アニメーションの追加と `getTransitionConfig` の更新
- `src/pages/Home.tsx`: ガチャ遷移時に `state` を渡すよう修正
- `src/pages/Gacha.tsx`: 戻るボタンと演出遷移時に `state` を渡すよう修正
- `src/pages/GachaEffect.tsx`: 結果画面への遷移時に `state` を渡すよう修正
- `src/pages/GachaResult.tsx`: ホームおよびガチャへの戻り時に `state` を渡すよう修正
