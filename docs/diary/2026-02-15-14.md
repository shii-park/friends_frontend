# 2026-02-15 開発日記 (14)

## 実施内容
- バトル関連画面への遷移に横スライドアニメーションを追加
- `App.tsx` の `pageVariants` を拡張し、`x` 軸（横方向）のアニメーションに対応
- `transitionConfig` に `type` (vertical/horizontal/fade) を導入し、遷移先に応じてアニメーションを使い分けられるように修正
- ホーム ↔ バトル選択 ↔ バトル準備 ↔ バトル ↔ バトル結果 の一連の流れで右へのスライド（および戻る際の左スライド）を実装

## 変更ファイル
- `src/App.tsx`: `pageVariants` と `getTransitionConfig` のロジックを大幅に拡張
- `src/pages/Home.tsx`: バトル選択への遷移時に `state` を渡すよう修正
- `src/pages/BattleSelect.tsx`: 戻るボタンと準備画面への遷移時に `state` を渡すよう修正
- `src/pages/BattlePrepare.tsx`: 戻るボタンとバトル開始時に `state` を渡すよう修正
- `src/pages/Battle.tsx`: 決着時のリザルト遷移に `state` を渡すよう修正
- `src/pages/BattleResult.tsx`: ホームへ戻るボタンに `state` を渡すよう修正
